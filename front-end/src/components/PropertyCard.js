import React from 'react';

function PropertyCard({ property }) {
  // Logic to 'buy' a property with virtual drinks

  return (
    <div>
      <h3>{property.name}</h3>
      {/* Add your property card rendering and buy logic here */}
    </div>
  );
}

export default PropertyCard;
